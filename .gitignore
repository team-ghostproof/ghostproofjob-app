# Local dev/test artifacts — CI installs fresh (npm install in e2e.yml)
node_modules/
package-lock.json
test-results/
playwright-report/
screenshots/
# saved auth session (contains live Firebase tokens — never commit)
playwright/.auth/
# local credentials (see .env.example)
.env
# firebase emulator artifacts
firebase-debug.log
firestore-debug.log
ui-debug.log
.firebase/

# python bytecode
scripts/__pycache__/
__pycache__/

# growth: generated content packs are per-run founder deliverables (Action uploads them as artifacts)
content-packs/
pw*.json
